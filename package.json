{
  "name": "y",
  "version": "1.0.0",
  "description": "to initialize: `npm install` to start: `npm start` GOTO: http://localhost:5000/",
  "main": "index.js",
  "scripts": {
    "start": "npx serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serve": "^11.1.0"
  }
}
